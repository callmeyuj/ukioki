# uki oki 品牌官网 — 项目记忆

> 每次新会话开始时，请先阅读本文件了解项目进展。

---

## 项目概述

**uki oki** 品牌官网，包含多个独立功能模块，部署于 `www.ukioki.com`。

### 功能模块

| 模块 | 路径 | 功能 | 状态 |
|------|------|------|------|
| **热量计算器** | `/calories/` | 宠物每日摄入热量计算 + uki oki 鲜食喂食建议 | ✅ 已上线（V3.24，数据源迁移至 shared/utils.js） |
| **订阅计划** | `/plan/` | 价格计算器（名义订阅）：固定包数 + 口味分配 + 折扣阶梯 → 参考预算 | ✅ V1.2 完成（千位符 bug 修复 + 跨模块去重），待打 tag 上线 |

### 技术栈

- 纯前端（HTML + CSS + JS），无框架依赖
- 每个模块独立运行，共享品牌资源
- **微信生态优先**：网站跳转场景主要在微信内，所有模块必须兼容微信内置浏览器、小程序 webview 容器（避免使用微信 webview 不支持的 API，分享等能力需按微信环境适配）
- 微信小程序可单独跳转各功能页

---

## 目录结构

```
ukioki/
├── shared/                # 共享品牌资源
│   ├── brand.css          # 品牌变量、基础重置、容器样式
│   ├── components.css     # 公共组件（按钮、卡片、输入框、Toast）
│   └── utils.js           # 共享工具函数（数值、环境判断、DOM、防抖）
├── calories/              # 热量计算器模块
│   ├── index.html         # 页面结构
│   ├── style.css          # 模块专属样式
│   ├── script.js          # 逻辑
│   ├── CLAUDE.md          # 模块专属文档
│   └── CHANGELOG.md       # 模块版本记录
├── plan/                  # 订阅计划模块（需求定义中）
│   ├── CLAUDE.md          # 模块快照（业务规则待明确清单）
│   └── CHANGELOG.md       # 模块版本记录（尚未发布）
├── CLAUDE.md              # 本文件（品牌级文档）
├── harness.md             # 品牌级迭代规范
└── CHANGELOG.md           # 品牌级版本记录
```

---

## 开发规范

详见根目录 `harness.md`，核心要点：
- 所有修改先进入 Plan 模式沟通
- 分步执行，每步等确认
- 每次版本变更同步更新 CHANGELOG.md
- Git tag 标记版本，保留最近 10 个

---

## 开发环境

- **本地服务器**：`http-server -p 8080 -c-1`（Node.js，禁用缓存）
- **访问地址**：
  - 热量计算器：`http://localhost:8080/calories/`
  - 订阅计划：`http://localhost:8080/plan/`（待开发）

### Git Remote

| Remote | 地址 | 用途 | 当前状态 |
|--------|------|------|----------|
| `ukioki` | `git@github.com:callmeyuj/ukioki.git` | 品牌官网（www.ukioki.com） | V1.3（plan/ V1.2 + calories/ V3.24） |

**部署**：腾讯云托管，域名 `www.ukioki.com` 已备案

---

## 当前版本

**V1.3** — 2026/09/22（shared/utils.js 建立单一数据源：共享 `PRODUCT_DATA`/`PET_CONFIG` + 新增 `formatMoney`/`formatDate`/`getFlavors`/`getAvgKcal`；plan/ V1.2 修复千位符 bug + 提取 `calcPriceSummary` 去重；calories/ V3.24 迁移至共享数据源，行为零变化）

---

## 模块状态

### 热量计算器（calories/）

**当前版本**：V3.24
**核心功能**：
- 引导用户选择宠物信息（犬/猫）
- 计算每日建议摄入热量（MER）
- 给出 uki oki 品牌鲜食喂食建议
- 支持分享功能（快照截图、保存图片、转发好友）

**详细文档**：见 `calories/CLAUDE.md`

### 订阅计划（plan/）

**当前版本**：✅ V1.2 完成（2026/09/22，3 屏向导 + 快照 + calories/ 联动 + 跨模块去重 + 千位符 bug 修复，待打 tag 上线，详见 `plan/CLAUDE.md`）
**定位**：名义订阅，实质**价格计算器**（不含配送周期/订阅管理/支付）
**核心流程**（八步）：
- 宠物类型（第 0 步）：犬默认选中，猫置灰 +"敬请期待/即将上线"备注（无独立猫落地页）
- 入口：手动输入每日包数；不知道 → 引导去 calories/（?from=plan&pet=dog，跳过其犬猫选择），算完经条件回程按钮自动带回数据（calories/ 不做正向引流入口）
- 锚点卡片：选天数（30/60/90 或自定义）→ 参考总包数 = ⌈每日包数 × 天数⌉，可一键采用或自由修改
- 确定总包数（≥30 包起订）→ 选配送次数（免邮额度 = ⌊总包数 ÷ 30⌋）
- 自由分配各口味包数 → 热量校验软提示（仅跳转路径）
- 折扣阶梯（整单单一折扣率）→ 参考预算 + 保存方案快照（唯一结果动作，兼作客服沟通介质）

---

## 共享资源架构（V1.1 新增，V1.2 扩展 JS）

### 架构设计

采用**方案 C（混合架构）**：共享品牌资源 + 模块专属样式/逻辑

```
shared/brand.css        ← 品牌变量、基础重置、容器
shared/components.css   ← 公共组件（按钮、卡片、输入框、Toast）
shared/utils.js         ← 共享工具函数（V1.2 新增）
calories/style.css      ← 热量计算器专属样式
calories/script.js      ← 热量计算器专属逻辑
plan/style.css          ← 订阅计划专属样式（待开发）
```

### shared/brand.css（品牌基础）

包含：
- CSS 变量（品牌色、背景、边框、文字、渐变、过渡）
- 基础重置（*, body）
- 容器样式（.container）

### shared/components.css（公共组件）

包含：
- 按钮系统（.btn, .btn-back, .btn-next, .btn-restart）
- 选项卡片（.option-btn 及子元素）
- 输入框（.input, .weight-input）
- Toast 提示（.toast）

### 使用方式

各模块在 `index.html` 中按顺序引用：
```html
<link rel="stylesheet" href="../shared/brand.css">
<link rel="stylesheet" href="../shared/components.css">
<link rel="stylesheet" href="style.css">
```

**CSS 优先级规则**：
- 共享样式提供默认样式
- 模块专属样式可覆盖共享样式（使用更具体的选择器）
- 加载顺序：brand.css → components.css → style.css

### shared/utils.js（共享工具函数 + 共享数据，V1.2 新增，V1.3 扩展数据）

包含：
- 数值处理：`roundToHalf()`（取整到 0.5）、`formatPacks()`（包数格式化）
- 环境判断：`isMobile()`（UA 判断移动设备）
- DOM 工具：`toggleSelection()`（选项卡片选中切换）
- 函数控制：`debounce()`（防抖，为 plan/ 输入场景备用）
- 格式化工具（V1.3 新增）：`formatMoney(v)`（千位分隔金额）、`formatDate(date)`（YYYY/MM/DD）
- 共享数据（V1.3 新增，建立单一数据源）：
  - `PRODUCT_DATA`（Object.freeze）：犬猫口味列表，含 id/name/grams/kcal/price
  - `PET_CONFIG`（Object.freeze）：宠物 icon/label
  - `getFlavors(petType)`：获取口味列表
  - `getAvgKcal(petType)`：动态计算平均单包热量（无需维护单独常量）

**JS 加载方式**（普通 script + defer，无构建工具）：
```html
<script src="../shared/utils.js" defer></script>
<script src="script.js" defer></script>
```
- defer 保证按顺序执行：utils.js 先于模块 script.js
- 函数为全局函数，模块内直接调用（函数名不冲突即可）
- 提取原则：仅提取**不依赖模块内部状态**的通用函数（如 `getPetConfig()` 依赖 calories 的 state，不提取）

### 优势

- ✅ **品牌一致性**：所有模块共享品牌变量和基础样式
- ✅ **代码复用**：公共组件和工具函数无需重复编写
- ✅ **易于维护**：品牌升级只需修改 shared/ 目录
- ✅ **模块独立**：各模块可灵活覆盖共享样式

---

## 整体规划（Roadmap）

### 优先级排序

| 优先级 | 功能 | 状态 | 说明 |
|--------|------|------|------|
| **P0** | 订阅计划（plan/） | ✅ V1.2 完成，待打 tag 上线 | 价格计算器：固定包数 + 口味分配 + 折扣阶梯 → 参考预算（详见 plan/CLAUDE.md） |
| **P1** | 官网主页 | 📋 待规划 | 根目录 index.html，衔接各模块入口 |
| **P2** | 用户登录系统 | 📋 待规划 | 微信登录 + 手机号登录 |
| **P3** | 微信小程序集成 | ❓ 待评估 | 继承登录态和数据（技术可行性待评估） |
| **P4** | 食谱配方功能 | 💭 待定 | 需求待明确 |

### 技术规划

**用户登录方案**：
- 100% 使用微信登录 / 手机号登录
- 小程序跳转网页：尝试继承登录态（UnionID 机制）
- 降级方案：网页和小程序分开登录，以手机号关联合并后台数据

**后端需求**：
- 用户数据存储服务（用户信息、宠物档案、订阅记录）
- 微信开放平台对接（UnionID、OAuth2.0）
- 支付接口（订阅付费，如需要）

---

## 待办功能

> **执行顺序（2026/09/22 确认）**：官网主页 → UI 升级 → 用户登录
> 
> **决策理由**：
> 1. **官网主页优先**：纯前端快速交付（1-2 周），不依赖 UI 升级方向，结构稳定（导航逻辑不随 UI 变化）
> 2. **UI 升级次之**：方向明确后一次性升级所有模块，避免重复工作，用户登录直接用新 UI
> 3. **用户登录最后**：UI 已稳定不需重做，有主页承载入口，技术复杂度高需充分准备

### 1. 订阅计划模块（P0）✅ 已完成
- 范围：plan/ 模块（价格计算器定位）
- 进度：✅ **V1.2 完成**（2026/09/22：跨模块架构优化 + 千位符 bug 修复 + 单一数据源迁移）
- 状态：**✅ 已打 tag（plan-v1.2），待部署上线**
- 后续阶段：用户账户登录 + 支付 + 配送（V2+ 规划）

### 2. 官网主页（P1）🎯 下一步
- 范围：根目录 index.html
- 功能：品牌展示 + 各模块入口导航（calories/plan/）
- 优先级：**最高**（快速交付价值，为后续功能提供载体）
- 预计工期：1-2 周
- 状态：**待规划**
- 依赖：无（独立进行）

### 3. UI 升级（P1.5）🎨 第二阶段
- 范围：全局（shared/brand.css + shared/components.css + 所有模块）
- 方向：向苹果官网学习（具体学习目标待明确，可能会变化）
- 优先级：**中**（官网主页完成后进行）
- 预计工期：2-4 周
- 状态：**待规划**
- 依赖：官网主页完成（避免重复工作）
- 风险：学习目标变化可能导致方向调整

### 4. 用户登录系统（P2）🔐 第三阶段
- 范围：shared/ 或独立模块
- 功能：微信登录、手机号登录、用户数据管理
- 优先级：**中低**（UI 升级完成后进行）
- 预计工期：1-2 个月（涉及后端 + 微信生态）
- 状态：**待规划**
- 依赖：UI 升级完成（登录页面直接用新 UI，避免重做）

### 5. 微信小程序集成（P3）
- 范围：跨平台登录态同步
- 方案：UnionID 机制 或 手机号关联
- 状态：**待评估**（依赖用户登录系统）

### 6. 食谱配方功能（P4）
- 范围：待定
- 功能：待定
- 状态：**待定**

---

## 最近修改记录

| 时间 | 内容 |
|------|------|
| 2026/09/22 | **品牌 V1.3 完成 + 后续规划确认**：① V1.3 跨模块架构优化（shared/utils.js 单一数据源 + plan/ V1.2 + calories/ V3.24），已打 tag 推送远程 ② 后续开发顺序确认：**官网主页 → UI 升级 → 用户登录**（官网主页优先快速交付，UI 升级方向明确后一次性到位，用户登录最后避免重做） ③ plan/ V1.2 待部署上线 |
| 2026/09/22 | **品牌 V1.3 跨模块架构优化**：① shared/utils.js（43→98 行）新增 `PRODUCT_DATA`/`PET_CONFIG`（Object.freeze 单一数据源）、`getFlavors(petType)`/`getAvgKcal(petType)`（动态算均值，无需维护单独常量）、`formatMoney(v)`（千位分隔符）、`formatDate(date)` ② plan/ V1.1→V1.2：删除本地 `PRODUCT_DATA`，改用共享数据（8 处）；新增 `calcPriceSummary()` 消除 renderPrice/renderSnapshot 重复计算；`fmtMoney` 改用共享 `formatMoney`（修复千位符 bug）；`formatDate` 替换 2 处重复日期格式化；CSS 死代码清理（`.text-price`/`.snapshot-total-right`/重复 hover 规则）+ 特异性补丁清理 ③ calories/ V3.23→V3.24：删除本地 `PET_CONFIG`/`PRODUCT_DATA`（含平均行）；`renderBrandSuggestions()` 改用 `getFlavors()` + 动态追加平均行（`getAvgKcal()`）；行为零变化 |
| 2026/09/21 | **plan/ V1.0 完成**：3 屏向导式价格计算器落地（Screen 1 基础配置 + 模拟计算 / Screen 2 口味分配 + 实时算价 / Screen 3 方案快照 + html2canvas 截图保存）；核心功能：订阅总数（≥30 包起订 + 包单位）、免邮实时计算、一键均分（余数优先便宜口味）、折扣阶梯、热量偏差校验（仅回程）、方案快照（企微客服二维码）；按钮文案优化（口味分配 → / 方案结果 →）+ 复用 calories/ 箭头图；UI 打磨（折叠 icon 柔和底色、猫敬请期待加深、口味单价、自定义时长小字、弹窗统一提示）；calories/ V3.23 联动完成（from=plan 条件隔离、跳过 Step 0、Step 1 返回订阅计划、Step 10 商城替换、回程带参数） |
| 2026/09/21 | **calories/ V3.23 plan/ 回程入口适配**：条件隔离（`from=plan` URL 参数），跳过 Step 0 锁定犬型，Step 10 商城按钮条件替换为「↩ 返回订阅计划」（带 pet/packs/kcal 回跳参数）。正常入口零感知。涉及 script.js（~25 行新增）+ index.html（1 按钮新增） |
| 2026/09/16 | **会话结束总结**——已完成：plan/ **需求定义阶段收官**（决策 #21~#31 共 11 项新增 + 6 项修订：运费规则/免邮公式、输出形态快照化、calories/ 联动机制（正向不引流 + 条件回程按钮替换 + 跳过犬猫选择）、宠物类型区分（犬默认/猫置灰）、免邮文案口径；否决 .py 后端与独立计算页路线并归档理由）。3 个 commit 已推送（6a712b6 / 09c8039 / 73191cd）。待办：① 阶段 2 方案设计（页面流程图、PLAN_RULES 计算设计、快照版式、calories/ 改动影响评估 + 回归测试、Plan 模式评审）② 预留观察项：配送次数输入环节可能废除（可拆卸设计） |
| 2026/09/16 | plan/ **免邮对外文案确认**（决策 #31）：页面免邮说明统一为"最低30包起送免邮"，与内部公式（每满 30 包免邮一次）同一规则两种表述；预留观察项：配送次数输入环节暂保留、后续可能废除交客服，设计上做成可拆卸模块 |
| 2026/09/16 | plan/ **热量计算路线确认**：否决 .py 后端与独立计算页，维持条件小改 calories/（决策 #30）；回程按钮改为"商城按钮条件替换"（from=plan 时 🛒 前往商城 → ↩ 返回订阅计划，正常入口零感知，#27 修订）；全链路同域 www.ukioki.com |
| 2026/09/16 | plan/ **宠物类型区分确认**：plan/ 第 0 步犬默认选中、猫置灰 +"敬请期待"备注（取代猫落地页，决策 #28）；回程跳转带 pet=dog，calories/ 对 from=plan 来源跳过 Step 0（犬锁定，决策 #29）；calories/ 改动扩至约 30~40 行，方案设计阶段做影响评估 |
| 2026/09/16 | plan/ **模块联动机制确认**：calories/ 不做正向入口（保留工具属性，决策 #26）；回程用条件回程按钮（plan/ → calories/?from=plan → 喂食量页条件显示"带结果返回"→ 回跳带 pet/packs/kcal，决策 #27）；calories/ 将有一次约 20~30 行的小改动（方案设计阶段细化，届时做影响评估 + 回归测试） |
| 2026/09/16 | plan/ **输出形态确认（#9 解决）→ 需求定义阶段收官**：快照为结果区唯一动作（plan/ 无档案能力，快照即"存档"；复用 calories/ html2canvas 方案）；客服/商城入口 V1 不做，超免邮引导"保存快照发给客服"；决策 #1~#25 全部落定，下一步进入方案设计 |
| 2026/09/16 | plan/ **运费规则确认**（#8 配送费解决）：免邮次数 = ⌊总包数 ÷ 30⌋，用户自选配送次数、系统实时对比额度，超额度显示预算 + "运费请联系客服"；`DISCOUNT_RULES` 升级为 `PLAN_RULES`（含 `packsPerFreeDelivery`）；主流程更新为八步；待明确仅剩输出形态 #9 |
| 2026/09/16 | plan/ 需求定义推进：**折扣阶梯确认**（按总包数 4 档：[30,90) 0.85 / [90,150) 0.80 / [150,210) 0.75 / ≥210 0.70，30 包起订，整单单一折扣率，不做凑单提示）；**引导策略确认**（"锚点 + 自由"：引入天数维度 30/60/90 快捷档 + 自定义，锚点 = ⌈每日包数 × 天数⌉ 向上取整，一键采用或自由修改）；**每日包数口径确认**（平均热量口径：犬 140 / 猫 119 kcal/包，与 calories/"平均数据"行一致；分配后做热量偏差校验软提示，仅限跳转路径）；计算主流程更新为七步；**猫延展性确认**（V1 仅犬，`PRICE_TABLE` 按 petType 配置驱动预留 cat；calories/ 跳转入口犬猫都显示，猫用户进 plan/ 引导到"即将上线"落地页）；待明确项仅剩配送费、输出形态 |
| 2026/09/08 | **会话结束总结**——已完成：① 补打历史 tag（v1.0/v1.1/calories-v3.22）② V1.2 共享 JS 工具提取（shared/utils.js，已推送）③ 文档架构调整（删 calories/harness.md，规范统一到品牌级，已推送）④ 旧仓库 Pet_Calories_Cal 迁移说明补提交（已推送双远程）⑤ plan/ 需求定义（定位价格计算器、犬类价格数据，commit 702c97c 待推送）。待办：用户整理业务方案（折扣阶梯/配送费/输出形态）后进入 plan/ 方案设计 |
| 2026/09/08 | plan/ 需求定义启动：创建模块文档骨架；定位澄清为价格计算器（固定包数+口味分配+折扣阶梯→参考预算）；品牌级新增"微信生态优先"技术约束（内置浏览器+小程序 webview 兼容） |
| 2026/09/08 | 文档架构调整：删除 calories/harness.md（迭代规范统一到品牌级），根 harness.md 吸收背景前言并修正第十二条（模块标配 = CLAUDE.md + CHANGELOG.md）；纯文档变更，版本保持 V1.2 |
| 2026/09/08 | V1.2 共享 JS 工具提取：创建 shared/utils.js（5 个函数），calories/script.js 精简（774→758 行），测试通过（7 断言 + 手动全流程）；补打历史 tag（v1.0/v1.1/calories-v3.22） |
| 2026/09/07 | **会话结束总结**：完成 V1.1 CSS 架构重构，测试通过，待办：提取共享 JS 工具函数 |
| 2026/09/07 | V1.1 CSS 架构重构（方案 C）：创建 shared/ 目录，提取品牌变量和公共组件，重构 calories/style.css（1141→872 行，-24%） |
| 2026/09/07 | 更新整体规划（Roadmap）：订阅计划（P0）、官网主页（P1）、用户登录（P2）、小程序集成（P3）、食谱功能（P4） |
| 2026/09/07 | V1.0 Monorepo 初始化，创建 ukioki 品牌官网结构，包含热量计算器（V3.22） |
