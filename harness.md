# uki oki 品牌官网 — 迭代行为规范

> 本文档是 ukioki monorepo 的**唯一**迭代行为守则，适用于所有模块（calories/、plan/ 等），模块内不再单独维护 harness.md。
>
> 背景：我不是程序员，对于代码无法直接理解，需要 claude 帮助产出代码。这个文档是给到 claude 后续迭代修改的行为手册，设定了安全边界。以下规则会弹性修改，酌情增删，时常调整与更新。

---

## 第一条：Plan 模式优先

所有修改先进入 `/plan` 模式沟通，不能直接修改源代码。沟通清楚后再执行，需要把步骤简单科普给到我，方便理解。

---

## 第二条：影响范围评估

修改核心工具函数前，必须先在整个项目里搜索所有调用它的地方，列出影响范围。

**Monorepo 特别注意**：
- 修改 `calories/` 或 `plan/` 内的代码，影响范围仅限于该模块
- 如果要提取共享资源到根目录，需要评估所有模块的兼容性

---

## 第三条：测试指令

每次修改后，必须提供简短的测试指令让我手动验证。

---

## 第四条：分步执行

禁止一次性修改超过 3 个相互依赖的文件，必须分步执行，每步都等我确认。

---

## 第五条：分级测试要求

修改代码前后，根据改动大小执行对应的测试要求：

| 改动级别 | 定义 | 测试要求 |
|----------|------|----------|
| **小改动** | ≤20 行 CSS/HTML/文案修改 | 提供手动验证步骤即可 |
| **中改动** | 1-2 个功能模块调整，或 >20 行的 CSS/HTML 修改 | 写 1 个简单测试用例 + 手动验证 |
| **大改动** | 新增功能模块，或代码改动 >200 行 | 修改前后各跑完整测试用例集 |

**注意**：改动行数按「新增 + 修改」的总行数计算，不含删除行。

---

## 第六条：版本号管理

### 模块版本号
每个模块（calories/、subscription/ 等）有独立的版本号，规则如下：

增加版本记录，从 V1.0 开始，版本号按十进制叠加推进：

| 改动级别 | 版本号增量 | 示例 |
|----------|-----------|------|
| 小改动 | +0.01 | V1.00 → V1.01 |
| 中改动 | +0.1 | V1.01 → V1.1 |
| 大改动 | +1.0 | V1.1 → V2.1 |

**边界情况处理原则**：按最大类别取
- 例：修改了 30 行 CSS + 调整了 1 个功能模块 → 均属于中改动 → +0.1
- 例：改动了 190 行代码但只是修 bug → 仍按大改动 → +1.0（行数优先）

**豁免项（不计入版本迭代）**：
- 纯 CSS 样式微调（颜色、间距、字体大小、动画效果等）
- 纯交互体验优化（按钮反馈、过渡动画、hover 效果等）
- 不涉及计算逻辑、功能模块变更的视觉类调整
- 仅调整参数值（如动画时长、透明度、尺寸数值等），未增删改代码结构

### 品牌版本号
根目录的版本号代表整体结构版本（如新增模块、重构目录结构等）。

---

## 第七条：版本回退与留存

- 使用 **Git tag** 标记每个版本（如 `v1.0`, `calories-v3.22`, `subscription-v1.0` 等）
- 至少留存最近 **10 个版本 tag**
- 回退操作 = `git checkout <tag>` 回到指定版本
- 超过 10 个版本时，删除最早的 tag，保留最近的 10 个

**Tag 命名规范**：
- 品牌级版本：`v1.0`, `v1.1`, `v2.0`
- 模块级版本：`calories-v3.22`, `subscription-v1.0`

---

## 第八条：异常处理

- 代码出错时 **立即停止**，不继续修改，先报告问题
- 回退版本后 **先验证功能**，确认正常再继续后续修改
- 不确定影响范围时，**优先询问用户**，不擅自决定

---

## 第九条：版本记录同步

### 模块级记录
每个模块有自己的 `CHANGELOG.md`，记录该模块的版本历史。

**同步规则**：
| 改动类型 | 更新位置 |
|----------|----------|
| 核心公式/系数规则变化 | 模块 CLAUDE.md + 模块 CHANGELOG.md |
| 步骤流程变化 | 模块 CLAUDE.md + 模块 CHANGELOG.md |
| 新增/删除关键函数 | 模块 CLAUDE.md + 模块 CHANGELOG.md |
| 产品数据变化 | 模块 CLAUDE.md + 模块 CHANGELOG.md |

### 品牌级记录
根目录的 `CHANGELOG.md` 记录品牌级变更（新增模块、重构目录等）。

**同步规则**：
| 改动类型 | 更新位置 |
|----------|----------|
| 新增/删除模块 | 根目录 CLAUDE.md + 根目录 CHANGELOG.md |
| 目录结构调整 | 根目录 CLAUDE.md + 根目录 CHANGELOG.md |
| 开发规范调整 | 根目录 harness.md + 根目录 CHANGELOG.md |
| 环境/配置变化 | 根目录 CLAUDE.md |

**豁免项**（不需要更新文档）：
- CSS 样式微调
- Bug 修复（不改变架构）
- 文案/注释修改

---

## 第十条：会话衔接规则

### 结束前
每次会话结束前，用户会要求总结本次对话内容。总结需包含：
- **已完成**：本次完成的功能/修改
- **待办**：未完成或下一步计划
- **时间戳**：精确到日期

**更新位置**：
- 如果改动在模块内：更新模块的 `CLAUDE.md` 的「最近修改记录」和「待办功能」
- 如果改动涉及品牌级：更新根目录的 `CLAUDE.md`

**Git 状态同步**：
1. 执行 `git fetch --all` 获取最新远程状态
2. 确认本地与远程的版本号一致

### 开始时
每次新会话开始时，**优先阅读根目录 `CLAUDE.md`**，了解：
1. 项目整体状态（有哪些模块、各自版本）
2. 上次会话的已完成/待办事项
3. 开发环境和关键配置

如果改动在特定模块内，再阅读该模块的 `CLAUDE.md`。

---

## 第十一条：Git 操作流程

### 基础概念

| 操作 | 作用 | 影响范围 |
|------|------|----------|
| `git add` | 把改动加入暂存区 | 仅本地 |
| `git commit` | 提交到本地仓库 | 仅本地 |
| `git push` | 把本地 commit 推送到远程 | 远程仓库 |
| `git pull` | 从远程拉取最新改动并合并 | 本地 |
| `git fetch` | 只获取远程信息，不合并 | 本地 |

### 推送规则

- 本地 commit 后，需要 `git push ukioki main` 才能同步到远程
- 推送前确认本地是最新的（`git fetch ukioki` 检查）

### 回退操作

- `git reset --hard ukioki/main` — 丢弃本地改动，回退到远程版本
- 回退前确认是否有未保存的改动
- 回退后需重新同步文档记录

### 远程 Repo 用途

- **ukioki** — 品牌官网（www.ukioki.com），腾讯云托管

---

## 第十二条：模块独立性原则

### 模块间隔离
- 每个模块（calories/、plan/）独立运行，不依赖其他模块的代码
- 模块标配文档：`CLAUDE.md`（模块快照）+ `CHANGELOG.md`（模块版本史）
- 迭代规范统一在品牌级（本文件），模块不单独维护 harness.md
- 模块的文档和代码自包含，便于单独维护

### 共享资源（shared/）
共享品牌资源已建立在 `shared/` 目录（V1.1 CSS、V1.2 JS）：
1. `brand.css` / `components.css` / `utils.js`
2. 各模块通过相对路径引用
3. 修改共享资源需评估所有模块的兼容性
4. `shared/` 是资源目录而非功能模块，不配置模块级文档（根 CLAUDE.md「共享资源架构」章节统一说明）

---

## 第十三条：Monorepo 特殊规则

### 模块级操作
- 修改某个模块时，只需关注该模块内的文件
- 模块的版本号独立管理
- 模块的测试独立进行

### 品牌级操作
- 新增模块：更新根目录 CLAUDE.md + 创建模块骨架
- 重构目录：更新根目录 CLAUDE.md + 通知所有模块
- 提取共享资源：评估所有模块兼容性，分步执行

### 文档层级
```
根目录 CLAUDE.md      ← 品牌级概览（模块列表、版本、环境）
├── calories/CLAUDE.md  ← 模块级详情（计算逻辑、代码地图）
└── plan/CLAUDE.md  ← 模块级详情（待创建）
```

阅读顺序：先看根目录，再深入具体模块。
